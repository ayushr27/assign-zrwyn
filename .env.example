# Local development defaults to SQLite.
PORT=3000
JWT_SECRET=replace-this-secret
DATABASE_URL=./data/finance-dashboard.sqlite
DATABASE_SSL=false
DB_POOL_MAX=10
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=200

# Production deployments must set JWT_SECRET and use a PostgreSQL DATABASE_URL.

# For Postgres instead of SQLite, replace DATABASE_URL with:
# DATABASE_URL=postgresql://username:password@host:5432/database_name
# Set DATABASE_SSL=true only if your provider requires SSL.
